/** Inline script — runs before paint to avoid light/dark flash. Default: dark (green premium). */
export const THEME_INIT_SCRIPT = `(function(){try{var k="nyumba-theme";var m=localStorage.getItem(k);var dark=true;if(m==="light"){dark=false;}else if(m==="system"){dark=window.matchMedia("(prefers-color-scheme: dark)").matches;}var r=document.documentElement;r.classList.toggle("dark",dark);r.classList.toggle("light-mode",!dark);var meta=document.querySelector('meta[name="theme-color"]');if(meta){meta.setAttribute("content",dark?"#0E0F14":"#4A2713");}}catch(e){document.documentElement.classList.add("dark");}})();`;
