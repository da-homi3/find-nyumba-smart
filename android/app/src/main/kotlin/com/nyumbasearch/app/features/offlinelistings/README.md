# Offline listings

WorkManager background sync for cached property cards on poor networks.

```kotlin
// implementation("androidx.work:work-runtime-ktx:2.10.0") // already in app/build.gradle.kts
```
