# LEGACY — not used for production deploy.
#
# NyumbaSearch ships via Nitro + TanStack Start:
#   npm run build  → dist/server/wrangler.json (generated)
#   npm run deploy → wrangler deploy --config dist/server/wrangler.json
#
# Source-of-truth for local env sync: wrangler.jsonc + scripts/sync-wrangler-env.mjs
# Secrets (service role, Gemini, etc.) live in Cloudflare dashboard / .env — not here.
#
# This file is kept for reference only (pre-Nitro KV/JWT prototype). Do not edit
# placeholder IDs below expecting them to affect Workers production.

name = "nyumba-search"
compatibility_date = "2026-05-31"
account_id = "2f9ae70dfb228529cc5e91d230ab2cfc"
main = "dist/server/index.mjs"
workers_dev = true
